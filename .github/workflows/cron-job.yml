name: Cron Job Schedule

permissions: read-all

on:
  workflow_dispatch:
  schedule:
    - cron: "5 * * * *"
    
jobs:
  run-cron-job:
    runs-on: ubuntu-latest

    steps:
      - name: hit-api
        run: curl -X POST https://gallery-app-vandraaa.vercel.app/cron-job
